# Dockerfile.backend

# 1. 베이스 이미지 선택 (파이썬 3.9 슬림 버전)
FROM python:3.9-slim

# 2. 작업 디렉토리 설정
WORKDIR /app

# 3. 필요한 라이브러리 설치
# 먼저 requirements.txt만 복사하여 라이브러리 설치 단계를 캐시합니다.
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4. 프로젝트 전체 코드 복사
COPY . .

# 5. 서버 실행
# 컨테이너 외부에서 접근할 수 있도록 --host 0.0.0.0으로 실행합니다.
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]